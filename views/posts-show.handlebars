{{> head}}

<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <a href="{{post.url}}" class="lead">{{post.title}}</a>
    
    <p>{{post.updateat}} {{post.summary}}</p>
  </div>
</div>

{{> tail}}